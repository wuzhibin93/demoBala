server:
  port: 8200

spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:/etc/envisual-server/db/sqlitedb
    username:
    password:
  jpa:
    database-platform: com.enlink.sqlite.SQLiteDialect
    show-sql: true
    hibernate:
      ddl-auto: update