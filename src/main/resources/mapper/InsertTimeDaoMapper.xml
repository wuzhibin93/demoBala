<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.enlink.de.elasticsearch.dao.InsertTimeDao">
    <insert id="insertTime">
    insert into inserttime (id,times,user,method,totaltime,totalsize,logtime,thatindextotalsize)
    VALUES(#{id},#{times},#{user},#{method},#{totaltime},#{totalsize},#{logtime},#{thatIndexTotalSize})
    </insert>
</mapper>